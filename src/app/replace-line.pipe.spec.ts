import { ReplaceLinePipe } from './replace-line.pipe';

describe('ReplaceLinePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceLinePipe();
    expect(pipe).toBeTruthy();
  });
});
