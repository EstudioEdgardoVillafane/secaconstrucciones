import { PrioritySelectPipe } from './priority-select.pipe';

describe('PrioritySelectPipe', () => {
  it('create an instance', () => {
    const pipe = new PrioritySelectPipe();
    expect(pipe).toBeTruthy();
  });
});
