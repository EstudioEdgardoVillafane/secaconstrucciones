import { FilterSecctionPipe } from './filter-secction.pipe';

describe('FilterSecctionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSecctionPipe();
    expect(pipe).toBeTruthy();
  });
});
