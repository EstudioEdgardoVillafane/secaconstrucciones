import { MostrarMitadStringPipe } from './mostrar-mitad-string.pipe';

describe('MostrarMitadStringPipe', () => {
  it('create an instance', () => {
    const pipe = new MostrarMitadStringPipe();
    expect(pipe).toBeTruthy();
  });
});
